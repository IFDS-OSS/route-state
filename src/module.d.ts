/**
 * @ifds/route-state Nuxt module.
 *
 * Add `'@ifds/route-state'` to the `modules` array in `nuxt.config.ts` — Nuxt
 * loads this module at build time. The user-facing composables and parsers are
 * auto-imported; their types live in `runtime/`.
 */
declare const _default: any
export default _default
